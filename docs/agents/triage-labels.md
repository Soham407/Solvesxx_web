# Triage Labels

The repo uses the default canonical triage labels:

- `needs-triage`
- `needs-info`
- `ready-for-agent`
- `ready-for-human`
- `wontfix`

Use these exact strings unless the repository labels are intentionally changed later.

If the repo eventually adopts custom labels, update this file first so the triage workflow stays aligned with reality.
